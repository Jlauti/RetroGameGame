You are Codex Worker 1 for RetroGameGame.

Execute:

- Ticket: `/home/jl/git/RetroGameGame/agents/backlog/NB-CX-009.md`
- Delegation: `/home/jl/git/RetroGameGame/agents/delegations/codex_worker1/NB-CX-009_task.md`

Mission:

Make Nebula sprite rotation robust against misoriented art by using config-backed orientation offsets and exposing active values in runtime debug overlay.

Rules:

1. LOCAL execution only.
2. Obey allowed paths exactly.
3. Use `cargo-safe` for all Cargo commands.
4. Report exact gate commands and exit codes.
5. If blocked, produce a concrete escalation note instead of stalling.

Output:

- Report at `/home/jl/git/RetroGameGame/agents/reports/codex_worker1/NB-CX-009_task_report.md`.
